<%= form_with(model: snippet_upload, local: true) do |form| %>
  <% if snippet_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snippet_upload.errors.count, "error") %> prohibited this snippet_upload from being saved:</h2>

      <ul>
        <% snippet_upload.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :files %>
    <%= form.file_field :files, multiple:true, accept: '.sql,.sqlpromptsnippet, text/plain' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
